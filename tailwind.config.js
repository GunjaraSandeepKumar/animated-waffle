module.exports = {
    darkMode: 'class', // Enables dark mode with a class toggle (instead of media-query based dark mode)
    content: [
      './src/**/*.{js,jsx,ts,tsx}', // Ensure Tailwind is watching your components
    ],
    theme: {
      extend: {},
    },
    plugins: [],
  }
  